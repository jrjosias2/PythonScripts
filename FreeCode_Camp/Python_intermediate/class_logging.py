#Tem muita coisa que pode ser parametrizada, procurar na documentacao do python por log-record attributes, criar o proprio logger também como boa pratica
#Voltar nessa aula mais pra frente, ou para futuras referências, nao faz parte dos conceitos e experiencias Core pro momento.
#Interessante demais parte do TimeRotatingFileHandler, Json logger tambem

import logging

logging.debug('Debug')
logging.info('info')
logging.warning('warning')
logging.error('error')
logging.critical('critical')